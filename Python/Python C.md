# Python C 層函數與型別總覽

## 1️⃣ 字串方法 (`str`) 的 C 層實作

Python 的內建型別大部分都是 C 實作的：

| 功能                                  | 說明                   | 實作層      |
| ------------------------------------- | ---------------------- | ----------- |
| `str.isalpha()`                       | 判斷是否全為字母       | C (CPython) |
| `str.isdigit()`                       | 判斷是否全為數字       | C           |
| `str.isalnum()`                       | 判斷是否全為字母或數字 | C           |
| `str.lower()` / `str.upper()`         | 轉大小寫               | C           |
| `str.startswith()` / `str.endswith()` | 前後字串判斷           | C           |
| `str.find()` / `str.index()`          | 查找子串               | C           |
| `str.replace()`                       | 取代子串               | C           |

---

## 2️⃣ 其他 C 層內建型別

| 型別    | 例子                                     | 實作層 |
| ------- | ---------------------------------------- | ------ |
| `list`  | `append()`, `pop()`, `extend()`          | C      |
| `dict`  | `get()`, `keys()`, `values()`, `items()` | C      |
| `set`   | `add()`, `remove()`, `intersection()`    | C      |
| `tuple` | 基本操作                                 | C      |

## 3️⃣ 數值與數學運算 (C 層實作)

| 型別 / 模組   | 功能                                   | 實作層 |
| ------------- | -------------------------------------- | ------ |
| `int`         | 基本算術運算、比較                     | C      |
| `float`       | 基本算術運算、比較                     | C      |
| `complex`     | 複數運算                               | C      |
| `math` 模組   | `sin()`, `cos()`, `sqrt()`, `log()` 等 | C      |
| `random` 模組 | `random()`, `randint()`, `choice()`    | C      |

---

## 4️⃣ 序列與迭代器 (C 層實作)

| 型別 / 模組   | 功能             | 實作層 |
| ------------- | ---------------- | ------ |
| `tuple`       | 索引、切片、迭代 | C      |
| `range`       | 生成序列、迭代   | C      |
| `enumerate()` | 迭代器生成       | C      |
| `zip()`       | 迭代器生成       | C      |
| `reversed()`  | 反轉迭代器       | C      |

---

## 5️⃣ 集合與映射 (C 層實作)

| 型別 / 模組 | 功能                                              | 實作層 |
| ----------- | ------------------------------------------------- | ------ |
| `set`       | `add()`, `remove()`, `union()`, `intersection()`  | C      |
| `frozenset` | 不可變集合操作                                    | C      |
| `dict`      | `get()`, `keys()`, `values()`, `items()`, `pop()` | C      |

---

## 6️⃣ 其他常用 C 層內建函數

| 函數              | 說明            | 實作層 |
| ----------------- | --------------- | ------ |
| `len()`           | 計算長度        | C      |
| `abs()`           | 絕對值          | C      |
| `min()` / `max()` | 找最小 / 最大值 | C      |
| `sum()`           | 加總            | C      |
| `all()` / `any()` | 邏輯判斷        | C      |
| `sorted()`        | 排序            | C      |
| `hash()`          | 計算雜湊值      | C      |
| `id()`            | 取得物件地址    | C      |

## 7️⃣ 字串處理 (C 層實作)

| 函數 / 方法                             | 說明               | 實作層 |
| --------------------------------------- | ------------------ | ------ |
| `str.split()`                           | 拆分字串           | C      |
| `str.join()`                            | 串接字串           | C      |
| `str.strip()` / `lstrip()` / `rstrip()` | 去除空白或指定字元 | C      |
| `str.capitalize()`                      | 首字母大寫         | C      |
| `str.title()`                           | 每個單字首字母大寫 | C      |
| `str.count()`                           | 計算子串出現次數   | C      |
| `str.swapcase()`                        | 大小寫互換         | C      |
| `str.islower()` / `str.isupper()`       | 檢查大小寫         | C      |

---

## 8️⃣ 編碼與位元運算 (C 層實作)

| 型別 / 函數                           | 說明                  | 實作層 |
| ------------------------------------- | --------------------- | ------ |
| `bytes` / `bytearray`                 | 底層二進位序列操作    | C      |
| `ord()`                               | 字元轉 Unicode 編碼   | C      |
| `chr()`                               | Unicode 編碼轉字元    | C      |
| `bin()` / `hex()` / `oct()`           | 進位轉換              | C      |
| `int.bit_length()`                    | 計算二進位長度        | C      |
| `int.to_bytes()` / `int.from_bytes()` | 整數與 bytes 互轉     | C      |
| `memoryview()`                        | 生成記憶體視圖        | C      |
| `encode()` / `decode()`               | 字串與 bytes 編碼解碼 | C      |
| `codecs.encode()` / `codecs.decode()` | 編碼轉換              | C      |

---

## 9️⃣ 迭代器與生成器 (C 層實作)

| 函數 / 型別   | 說明           | 實作層 |
| ------------- | -------------- | ------ |
| `iter()`      | 生成迭代器     | C      |
| `next()`      | 取得下一個元素 | C      |
| `range()`     | 整數序列生成   | C      |
| `enumerate()` | 帶索引迭代     | C      |
| `zip()`       | 串列或序列打包 | C      |
| `reversed()`  | 反轉序列       | C      |

---

## 🔟 其他常用內建函數 (C 層實作)

| 函數                 | 說明           | 實作層 |
| -------------------- | -------------- | ------ |
| `type()`             | 查看型別       | C      |
| `isinstance()`       | 型別檢查       | C      |
| `callable()`         | 是否可呼叫     | C      |
| `repr()` / `ascii()` | 取得字串表示   | C      |
| `id()`               | 物件記憶體地址 | C      |
| `hash()`             | 計算雜湊值     | C      |
| `all()` / `any()`    | 邏輯判斷       | C      |
| `len()`              | 長度計算       | C      |
| `sum()`              | 加總           | C      |
| `min()` / `max()`    | 找最小 / 最大  | C      |

## 11️⃣ 時間與日期 (C 層實作)

| 模組 / 函數                          | 說明             | 實作層       |
| ------------------------------------ | ---------------- | ------------ |
| `time.time()`                        | 取得 Unix 時間戳 | C            |
| `time.sleep()`                       | 暫停程式         | C            |
| `time.localtime()` / `time.gmtime()` | 轉換時間元組     | C            |
| `time.mktime()`                      | 時間元組轉時間戳 | C            |
| `datetime.datetime.now()`            | 取得現在時間     | C (部分底層) |
| `datetime.timedelta`                 | 時間差運算       | C (部分底層) |

---

## 12️⃣ 正則表達式 (C 層實作)

| 模組 / 函數                                   | 說明           | 實作層 |
| --------------------------------------------- | -------------- | ------ |
| `re.compile()`                                | 編譯正則表達式 | C      |
| `re.match()` / `re.search()` / `re.findall()` | 搜尋匹配       | C      |
| `re.sub()`                                    | 取代           | C      |
| `re.split()`                                  | 分割字串       | C      |

> 註：Python 的 `re` 模組底層大部分用 C 實作，速度比純 Python 快。

---

## 13️⃣ 檔案與系統操作 (C 層實作)

| 函數 / 模組                           | 說明            | 實作層   |
| ------------------------------------- | --------------- | -------- |
| `open()`                              | 開啟檔案        | C        |
| `os.stat()` / `os.listdir()`          | 檔案屬性與目錄  | C        |
| `os.remove()` / `os.rename()`         | 檔案操作        | C        |
| `os.path.exists()` / `os.path.join()` | 路徑操作        | C        |
| `shutil.copy()` / `shutil.move()`     | 複製 / 移動檔案 | C (部分) |

---

## 14️⃣ itertools 與高效迭代工具 (C 層實作)

| 函數 / 類別                                   | 說明           | 實作層 |
| --------------------------------------------- | -------------- | ------ |
| `itertools.count()`                           | 無限計數器     | C      |
| `itertools.cycle()`                           | 循環迭代器     | C      |
| `itertools.chain()`                           | 串聯多個迭代器 | C      |
| `itertools.islice()`                          | 迭代切片       | C      |
| `itertools.permutations()` / `combinations()` | 排列組合生成   | C      |
| `itertools.zip_longest()`                     | 多迭代器打包   | C      |

## 16️⃣ 壓縮與打包模組 (C 層實作)

| 模組 / 函數                             | 說明             | 實作層 |
| --------------------------------------- | ---------------- | ------ |
| `zlib.compress()` / `zlib.decompress()` | 壓縮 / 解壓      | C      |
| `gzip.open()` / `gzip.GzipFile()`       | gzip 壓縮檔操作  | C      |
| `bz2.BZ2File()`                         | bzip2 壓縮檔操作 | C      |
| `lzma.LZMAFile()`                       | LZMA 壓縮檔操作  | C      |

---

## 17️⃣ 散列與哈希 (C 層實作)

| 函數 / 模組                                     | 說明             | 實作層 |
| ----------------------------------------------- | ---------------- | ------ |
| `hash()`                                        | 物件雜湊值       | C      |
| `hashlib.md5()` / `hashlib.sha1()` / `sha256()` | MD5/SHA 雜湊計算 | C      |
| `hmac.new()`                                    | HMAC 計算        | C      |

---

## 18️⃣ 內建例外與型別檢查 (C 層實作)

| 函數 / 型別              | 說明         | 實作層 |
| ------------------------ | ------------ | ------ |
| `isinstance()`           | 型別檢查     | C      |
| `issubclass()`           | 子類檢查     | C      |
| `Exception` 基類         | 所有內建例外 | C      |
| `len()`, `abs()`, `id()` | 常用內建函數 | C      |

---

## 19️⃣ 高效 buffer 與底層資料結構 (C 層實作)

| 型別 / 函數                         | 說明              | 實作層 |
| ----------------------------------- | ----------------- | ------ |
| `array.array()`                     | 數組操作          | C      |
| `struct.pack()` / `struct.unpack()` | 二進位打包 / 解包 | C      |
| `memoryview()`                      | 記憶體視圖        | C      |
| `collections.deque`                 | 雙端佇列          | C      |
| `collections.Counter`               | 計數器            | C      |
| `collections.OrderedDict`           | 有序字典          | C      |
